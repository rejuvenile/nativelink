DEPLOYMENT BLOCKED — see Task #89.

The mirror-durability commit chain (f75ce645 + d0bac140 + 2b9c7111 +
66da0a6e + 598c45a5 + e479bb14) is on main but MUST NOT be deployed
to production until Task #89 resolves: the BlobsInStableStorage ack
pipeline is silently broken in production (294 stale-mirror-expiry
warnings on banjo alone in 3 days; receipt log NEVER fires).

Removing the 30s/120s sweeper (which this commit chain does) without
first fixing the ack pipeline cascades to fleet-wide mirror collapse:
mirror_blobs accumulates -> 2 GiB cap -> Saturated classification
doesn't quarantine -> picker keeps trying full peers -> tee fails
fleet-wide.

This file MUST exist on main until Task #89 is verified resolved AND
the cleanup chain has been deployed safely. Remove it as part of the
commit that resolves #89.

Created 2026-04-24.
