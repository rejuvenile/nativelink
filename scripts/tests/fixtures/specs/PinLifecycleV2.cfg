\* Pin v2 (in flight): durable pins, TTL release DISABLED.
\* Expected TLC outcome: INVARIANT VIOLATED on NoPermanentPinLeak.
\* Trace will show:
\*   WorkerPinAction(d1) -> WorkerUploadCacheHit(d1)
\*   ... and any (Pin, Fresh, Drain, Deliver) cycle for d2, d3 ...
\* Final state: pendingActionOutputs={}, stableDigestsQueue=<<>>,
\*              bisInFlight=<<>>, workerPins={d1} -> Quiescent /\ d1 leaked.
\*
\* This is the bug documented in
\* .claude/reviews/bis-coverage-for-already-cached-outputs/audit.md.

SPECIFICATION Spec

CONSTANTS
    Digests = {d1, d2, d3}
    PrimedDigests = {d1}
    PinV2 = TRUE

INVARIANTS
    TypeOK
    NoPermanentPinLeak

CHECK_DEADLOCK FALSE
