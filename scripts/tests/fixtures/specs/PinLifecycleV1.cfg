\* Pin v1 (current production): TTL self-heal active.
\* Expected TLC outcome: NO invariant violation. Liveness holds.

SPECIFICATION Spec

CONSTANTS
    Digests = {d1, d2, d3}
    PrimedDigests = {d1}
    PinV2 = FALSE

INVARIANTS
    TypeOK
    NoPermanentPinLeak

PROPERTIES
    EventuallyAllPinsReleased

CHECK_DEADLOCK FALSE
