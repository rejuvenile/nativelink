\* ATOMICITY BATCH D — de-atomized single-holder, CEILING INTACT.
\* The frozen RootHolders constant is replaced by a DYNAMIC rootHolders view moved
\* by async AcquireRoot/EvictRoot (models BlobsAvailable POST-materialize publish +
\* dir-cache eviction, incl. evicting the LAST holder). EXPECT: every safety
\* invariant HOLDS under full holder dynamics — the checked properties are
\* holder-identity-independent, so de-atomizing the holder set surfaces NO new bug.
\* The reachability witnesses (AsyncWitness*.cfg) prove the dangerous states ARE
\* exercised, so this HOLD is not vacuous.

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2"}
  Ops = {"o1", "o2", "o3"}
  WorkerOrder <- Order_w123
  InitRootHolders = {"w1"}
  Capacity = 5
  MaxScore = 1
  THRESHOLD = 101
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = FALSE
  BuggedStaleRankOnly = FALSE
  PrefFlat = FALSE
  GateActive = TRUE
  AllowEvict = FALSE
  MaxToggles = 1
  HolderBudget = 2
  PCoreCount <- PCoreCount_All1

INVARIANTS
  TypeOK
  I5_Bounded
  I4_HeterogeneitySafe
  I5_Freshness
  CapacityRespected
  EvictedEmpty
  NoWedge

PROPERTIES
  I6_RankerConsistency
  I5_BoundedOverride
  I1_PHeadroomFirst
