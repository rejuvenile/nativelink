\* FL-688 never-BIS-acked pin-leak — BUGGED regime.
\*
\* EventDrivenOnShortCircuit = FALSE: the `AlreadyExists` short-circuit
\* (running_actions_manager.rs:8284-8302) reaches server-durable WITHOUT
\* driving any durability event. Interleaving that leaks:
\*   1. WorkerWriteAndPin(d)     — worker holds+pins d indefinitely.
\*   2. WorkerAdvertise(d)       — one-shot BlobsAvailable sent.
\*   3. ServerHandleAdvert       — has_durably poll: d NOT yet durable -> miss.
\*   4. ServerBecomesDurableShortCircuit(d) — durable via AlreadyExists, NO BIS.
\*   => quiescent with workerPins = {d}: the pin is stuck forever.
\*
\* Expected TLC outcome: INVARIANT DurableHeldReleasedAtQuiescence VIOLATED
\* (a quiescent/deadlock state with a durable-but-still-pinned digest); the
\* temporal EventuallyAllReleased also fails.

SPECIFICATION Spec

CONSTANTS
    Digests = {d1, d2}
    EventDrivenOnShortCircuit = FALSE

INVARIANTS
    TypeOK
    StableFeedIsDurable
    DurableHeldReleasedAtQuiescence

PROPERTIES
    EventuallyAllReleased

CHECK_DEADLOCK FALSE
