\* FL-688 V3 — FIXED: advertise-on-pin wired. The BEFORE-pin case is BIS'd via
\* has_durably -> mark_stable (advertise); the AFTER-pin case is BIS'd via the
\* fresh write's GLOBAL broadcast (advertise or not). All invariants HOLD.
SPECIFICATION Spec
CONSTANTS
    Digests = {d1, d2}
    AdvertiseOnPin = TRUE
INVARIANT TypeOK
INVARIANT StableFeedIsDurable
INVARIANT DurableHeldReleasedAtQuiescence
CHECK_DEADLOCK FALSE
