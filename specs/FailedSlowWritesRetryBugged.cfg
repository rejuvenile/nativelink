\* BugMode = TRUE: drain-and-retry has no convergence mechanism
\* (no max-retry counter, no exponential backoff, no dead-letter
\* queue). Models current production at
\* local_worker.rs:1327-1352 + handle_upload_missing_blobs's
\* "log warn and drop the result" arm.
\*
\* MaxRetries is set to a non-zero value but never read under bug
\* (DrainAndRetryUnbounded ignores it).
\*
\* Expected TLC outcome:
\*   * LIVENESS VIOLATED on `EventuallyConverges`. TLC produces a
\*     cycle counter-example along the persistent-failure trace:
\*       PinTtlInsertOnHang -> failedSlowWrites = {"D"} ->
\*       Disconnect -> Reconnect -> DrainAndRetryUnbounded ->
\*       failedSlowWrites = {} BUT slowWrite = "InFlight" ->
\*       SlowWriteFailsInBand -> failedSlowWrites = {"D"} again ->
\*       Disconnect -> Reconnect -> ... infinite loop.
\*     The trace shape is exactly the production symptom.
\*   * Safety invariants TypeOK / NoSimultaneousReplay hold.

SPECIFICATION Spec

CONSTANTS
    MaxRetries = 2
    BugMode = TRUE

INVARIANTS
    TypeOK
    NoReinsertAfterAckAndUnpin

PROPERTIES
    EventuallyConverges

CHECK_DEADLOCK FALSE
