\* BUGGED: no atomic key claim. Two racing add_action on one key both
\* Sample a stale (empty) index and both mint distinct ops => dedup broken.
\* EXPECTED: AtMostOneLiveOp and CacheableShareOneOp VIOLATED.
SPECIFICATION Spec

CONSTANTS
    Clients = {c1, c2}
    AtomicKeyClaim = FALSE

INVARIANTS
    TypeOK
    AtMostOneLiveOp
    CacheableShareOneOp
