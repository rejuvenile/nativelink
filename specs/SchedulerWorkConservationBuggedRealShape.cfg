\* BUG REPRO at the REAL prod worker shape: p_core_count = 4, e_core_count = 6
\* (10 processors), 2 workers, 10 queued actions, RAM never binds.
\*
\* Fleet P-capacity = 2 * 4 = 8 < 10 actions, so at the fixpoint each worker
\* sits at running = 4 = p_core_count with all 6 E-cores idle and >=2 actions
\* still queued — an exact miniature of the live incident (queue deep, workers
\* pinned at 4, 6 E-cores idle, RAM not the bottleneck).
\*
\* EXPECT: WorkConserving VIOLATED, with the 12-idle-E-core (6/worker)
\* counterexample.

SPECIFICATION Spec

CONSTANTS
  Workers = {w1, w2}
  Actions = {a1, a2, a3, a4, a5, a6, a7, a8, a9, a10}
  PCoreCount = 4
  ECoreCount = 6
  RamCap = 1000
  ActionRamEach = 1
  GateMode = "PCoreOnly"

\* No SYMMETRY here: Permutations(Actions) with 10 actions = 10! > 1e6 elements
\* (TLC set-construction cap).  The raw state space (3^10) is tiny, so full
\* enumeration without symmetry is fast.

INVARIANTS
  TypeOK
  NoProcessorOversubscription
  NoRamOverflow
  WorkConserving
