\* (design §13 Refinement 2 — REACHABILITY WITNESS) Same topology as
\* SingleHolderCeiling. Runs the NEGATED invariant SoleHolderOverrideReached,
\* which MUST be VIOLATED. The violation trace is the POSITIVE witness that the
\* §13 R2 case is genuinely EXERCISED: a reachable state where the sole root-
\* holder w1 is an over-p_count override-admit (Running = 1 = PCoreCount, so
\* clause 2 `running < p_count` is false and it holds only via clause 3) and is
\* the ONLY cache-eligible worker, while the free-slot peers w2/w3 exist but do
\* NOT hold the root. This is what makes the SingleHolderCeiling coverage HONEST
\* rather than vacuously-empty: I6 is vacuous BECAUSE this case is reached, not
\* because the case never happens.
\*
\* EXPECTED: SoleHolderOverrideReached VIOLATED (trace shows w1 override-admit,
\* sole cache-eligible). A clean "No error" here = the topology never drove w1
\* over p_count = the cfg is a no-op (the tell to reject).

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2", "w3"}
  Ops = {"o1", "o2", "o3", "o4"}
  WorkerOrder <- Order_w123
  RootHolders = {"w1"}
  Capacity = 5
  MaxScore = 2
  THRESHOLD = 101
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = FALSE
  BuggedStaleRankOnly = FALSE
  PrefFlat = FALSE
  GateActive = TRUE
  AllowEvict = FALSE
  MaxToggles = 2
  PCoreCount <- PCoreCount_All1

INVARIANTS
  SoleHolderOverrideReached
