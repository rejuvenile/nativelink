\* FIXED: authoritative key-conditional claim (SETNX / per-key lock across
\* sample+insert) sees an op already created even before the RediSearch
\* index catches up => the second racer JOINS instead of minting.
\* EXPECTED: all invariants HOLD.
SPECIFICATION Spec

CONSTANTS
    Clients = {c1, c2}
    AtomicKeyClaim = TRUE

INVARIANTS
    TypeOK
    AtMostOneLiveOp
    CacheableShareOneOp
