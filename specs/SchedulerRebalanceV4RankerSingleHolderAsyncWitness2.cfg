\* ATOMICITY BATCH D — NON-VACUITY WITNESS (W2): sole-holder-over-ceiling residual
\* is STILL reached under holder dynamics. Runs the NEGATED reachability invariant
\* SoleHolderOverrideReached, EXPECTED TO BE VIOLATED. The trace shows the sole
\* cache-eligible worker is an over-p_count override-admit while free-slot peers
\* exist but do NOT hold the root — the §13 R2 case, confirmed reachable after
\* de-atomizing the holder set (so I5_Bounded's HOLD in AsyncFixed genuinely
\* governs this case, not vacuously).

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2"}
  Ops = {"o1", "o2", "o3"}
  WorkerOrder <- Order_w123
  InitRootHolders = {"w1"}
  Capacity = 5
  MaxScore = 1
  THRESHOLD = 101
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = FALSE
  BuggedStaleRankOnly = FALSE
  PrefFlat = FALSE
  GateActive = TRUE
  AllowEvict = FALSE
  MaxToggles = 1
  HolderBudget = 2
  PCoreCount <- PCoreCount_All1

INVARIANTS
  SoleHolderOverrideReached
