\* BugMode = FALSE: the post-fix flag (`has_actually_returned_some`)
\* is set ONLY in the real has-then-Some branch, NOT in the lazy
\* short-circuit branch. Models the production fix at
\* fast_slow_store.rs:920 + :966-969 (post-investigator
\* ae69e88918d055f5e).
\*
\* Expected TLC outcome: NO invariant violation. The PHANTOM BLOB
\* warn fires only when realHasReturnedSome is TRUE (i.e. has()
\* actually returned Some, then populate produced NotFound — the
\* genuine has-evict race the alert was designed to surface).

SPECIFICATION Spec

CONSTANTS
    BugMode = FALSE

INVARIANTS
    TypeOK
    WarnFiresOnlyOnGenuineRace
    GateFlagMatchesHasOutcome

CHECK_DEADLOCK FALSE
