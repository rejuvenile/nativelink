\* Producer always terminates the writer (success: send_eof; failure:
\* send_error). Models the post-WriteHalfGuard build at fast_slow_store.rs.
\* Expected TLC outcome: NO invariant violation, NO deadlock.

SPECIFICATION Spec

CONSTANTS
    BugMode = FALSE

INVARIANTS
    TypeOK
    JoinAlwaysCompletes

PROPERTIES
    EventuallyJoinReturns

\* "Returned" is the legitimate terminal state — TLC would otherwise
\* report it as a deadlock because it has no successor. The
\* JoinAlwaysCompletes safety invariant already catches the real
\* (intermediate) deadlock if the producer leaves the channel Open.
CHECK_DEADLOCK FALSE
