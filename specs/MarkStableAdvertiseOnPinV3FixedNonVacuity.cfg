\* FL-688 V3 — FIXED NON-VACUITY. Advertise ON. Runs AdvertReleaseUnreachable
\* as an INVARIANT EXPECTED TO BE VIOLATED: the counterexample is a durable-
\* BEFORE-pin digest fed to mark_stable BY the advertise-on-pin path and
\* released at quiescence. Proves Fixed's "HOLDS" is not vacuous — the
\* advertise mechanism actually fires and drives the leaking class to release.
\* Expect: AdvertReleaseUnreachable VIOLATED (the mechanism fires).
SPECIFICATION Spec
CONSTANTS
    Digests = {d1, d2}
    AdvertiseOnPin = TRUE
INVARIANT AdvertReleaseUnreachable
CHECK_DEADLOCK FALSE
