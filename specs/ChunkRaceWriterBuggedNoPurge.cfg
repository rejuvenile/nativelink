\* ChunkRaceWriterBuggedNoPurge.cfg — bugged variant: RaceWriterGuard's
\* purge_writer_in_flight is omitted on cancel/panic. Expected outcome:
\* TLC reports the NoOrphanedInFlight invariant violation.

SPECIFICATION Spec

CONSTANTS
    Writers = {w1, w2}
    Chunks = {c0, c1}
    AllowPwriteFail = TRUE
    AllowRunnerCancel = TRUE

INVARIANTS
    TypeOK
    NoOrphanedInFlight

CHECK_DEADLOCK FALSE
