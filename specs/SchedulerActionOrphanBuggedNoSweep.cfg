\* Bug repro config — KeepaliveSweep DISABLED.
\* Expected: TLC finds a counterexample violating NoOrphan
\* (Executing op whose worker doesn't have it AND no requeue).
\* Liveness EventualProgress should also fail.

SPECIFICATION Spec

CONSTANTS
  Workers = {w1, w2}
  Operations = {op1, op2}
  THRESHOLD = 2
  MaxTime = 8
  EnableSweep = FALSE

INVARIANTS
  TypeInv
  COMPOSITE_INVARIANT
