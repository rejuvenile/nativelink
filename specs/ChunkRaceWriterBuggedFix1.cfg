\* ChunkRaceWriterBuggedFix1.cfg — bugged variant: commit_running NOT
\* cleared on RunnerGuardDropCancelled. Expected outcome: TLC reports
\* the NoOrphanedRunner invariant violation as a counter-example trace.

SPECIFICATION Spec

CONSTANTS
    Writers = {w1, w2}
    Chunks = {c0, c1}
    AllowPwriteFail = TRUE
    AllowRunnerCancel = TRUE

INVARIANTS
    TypeOK
    NoOrphanedRunner

CHECK_DEADLOCK FALSE
