\* FL-688 V2 — BUGGED: no mark_stable in the ExistenceCacheStore durable-skip.
\* Expect: DurableHeldReleasedAtQuiescence VIOLATED (orphaned indefinite pin —
\* D durable via the skip, no BIS, stuck pinned at quiescence).
\* StableFeedIsDurable HOLDS (durability never weakened, even bugged).
SPECIFICATION Spec
CONSTANTS
    Digests = {d1, d2}
    MarkStableOnDurableSkip = FALSE
    FixGatesOnLocality = FALSE
INVARIANT TypeOK
INVARIANT StableFeedIsDurable
INVARIANT DurableHeldReleasedAtQuiescence
CHECK_DEADLOCK FALSE
