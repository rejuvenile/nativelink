\* FL-688 V3 — WITNESS (the crux of the correctness argument). Advertise is
\* OFF. Runs AfterPinReleaseUnreachable as an INVARIANT EXPECTED TO BE
\* VIOLATED: the counterexample is a durable-AFTER-pin digest reaching a
\* released, quiescent state WITHOUT advertise -> its release came from the
\* fresh write's GLOBAL BIS. Proves the after-pin case is advertise-
\* INDEPENDENT (so advertise-on-pin only needs to cover the before-pin case).
\* Expect: AfterPinReleaseUnreachable VIOLATED (the witness fires).
SPECIFICATION Spec
CONSTANTS
    Digests = {d1, d2}
    AdvertiseOnPin = FALSE
INVARIANT AfterPinReleaseUnreachable
CHECK_DEADLOCK FALSE
