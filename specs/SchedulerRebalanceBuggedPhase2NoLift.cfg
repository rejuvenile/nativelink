\* TEETH: Bug = Phase2NoLift — the gate NEVER lifts (CacheTierViable requires
\* HasPHeadroom unconditionally) AND the botched impl also gated the fallback,
\* so when every worker is at p_count NO path can dispatch => wedge.
\* Fleet: 2 workers p_count=1, capacity=2, 3 ops (MORE ops than total
\* P-capacity, so a 3rd op stays Queued once both workers are P-full). Once
\* each worker holds 1 action (at p_count) the 3rd op can never dispatch even
\* though each worker has a FREE physical slot (E core). EXPECT:
\*   - NoWedge VIOLATED: a Queued op + a worker that can accept, but NO
\*     dispatch action enabled (the write-once-dispatch-never wedge — the
\*     scheduler analogue of the 2026-05-08 debacle cell).
\*   - EventuallyDispatched VIOLATED: the op stays Queued forever.
\* No evict / no load-sat — the wedge is pure gate logic.

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2"}
  Ops = {"o1", "o2", "o3"}
  Capacity = 2
  AllowEvict = FALSE
  AllowLoadSat = FALSE
  MaxToggles = 0
  Bug = "Phase2NoLift"
  PCoreCount <- PCoreCount_All1

INVARIANTS
  TypeOK
  I4_HeterogeneitySafe
  I5_Freshness
  CapacityRespected
  NoWedge

PROPERTIES
  EventuallyDispatched
