\* ATOMICITY BATCH D — de-atomized single-holder, CEILING DROPPED (teeth).
\* Identical to AsyncFixed EXCEPT DropCeiling = TRUE. Proves the FACTOR ceiling is
\* the genuine recovery mechanism and that it STILL bites under full holder
\* dynamics — i.e. de-atomizing the holder set did not remove the need for the
\* ceiling, nor does holder churn provide an alternative brake. With the ceiling
\* gone and THRESHOLD=101 (PReportedIdle always TRUE), the sole holder w1 stays
\* override-eligible forever, the cache regime keeps routing same-root ops to it,
\* and its Running climbs past the ceiling (1*2 = 2).
\*
\* EXPECT: I5_Bounded VIOLATED (w1 Running reaches 3 > 2). A clean "No error"
\* would mean the topology fails to drive the residual.

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2"}
  Ops = {"o1", "o2", "o3"}
  WorkerOrder <- Order_w123
  InitRootHolders = {"w1"}
  Capacity = 5
  MaxScore = 1
  THRESHOLD = 101
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = TRUE
  BuggedStaleRankOnly = FALSE
  PrefFlat = FALSE
  GateActive = TRUE
  AllowEvict = FALSE
  MaxToggles = 1
  HolderBudget = 2
  PCoreCount <- PCoreCount_All1

INVARIANTS
  TypeOK
  I5_Bounded
