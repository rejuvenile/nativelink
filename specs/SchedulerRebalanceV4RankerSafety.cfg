\* V4 RANKER — the FIXED ranker (p_headroom_pref PRIMARY key). Headline: I6
\* HOLDS under the ADVERSARIAL stale-low LoadScore adversary — an override-admit
\* NEVER beats a genuine-free-slot eligible worker, for ANY LoadScore assignment
\* (including loadScore pinned LOW on the override worker while its Running climbs).
\* This is the go/no-go for design §12.
\*
\* Topology: PCoreCount_All1 => override ceiling = 1*2 = 2; Capacity = 3 > 2 so
\* the FRESH-count ceiling (not the slot cap) binds the override. 3 ops, 3 workers.
\* THRESHOLD = 50 (idle=0 < 50 <= busy=100) so clause 3 CAN fire (override active).
\* GateActive = TRUE. Full adversary: Evict + StaleLoadScore + StalePLoad on.
\* AdmitAllThreshold = FALSE, DropCeiling = FALSE (real gate). BuggedStaleRankOnly
\* = FALSE, PrefFlat = FALSE (the FIXED ranker).

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2", "w3"}
  Ops = {"o1", "o2", "o3"}
  WorkerOrder <- Order_w123
  Capacity = 3
  MaxScore = 2
  THRESHOLD = 50
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = FALSE
  BuggedStaleRankOnly = FALSE
  PrefFlat = FALSE
  GateActive = TRUE
  AllowEvict = TRUE
  MaxToggles = 3
  PCoreCount <- PCoreCount_All1

INVARIANTS
  TypeOK
  I4_HeterogeneitySafe
  I5_Freshness
  CapacityRespected
  EvictedEmpty
  NoWedge
  PrefMonotone

PROPERTIES
  I6_RankerConsistency
  I5_BoundedOverride
  I1_PHeadroomFirst
  I3_LocalityBounded
