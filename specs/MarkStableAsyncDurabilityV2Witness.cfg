\* FL-688 V2 — WITNESS (non-vacuity): same toggles as Fixed. Runs
\* SkipReleaseUnreachable as an INVARIANT EXPECTED TO BE VIOLATED. The
\* counterexample is the proof that the durable-skip fix path genuinely
\* drives a digest to a released, quiescent state (Fixed is not vacuous).
\* Expect: SkipReleaseUnreachable VIOLATED (the witness fires).
SPECIFICATION Spec
CONSTANTS
    Digests = {d1, d2}
    MarkStableOnDurableSkip = TRUE
    FixGatesOnLocality = FALSE
INVARIANT SkipReleaseUnreachable
CHECK_DEADLOCK FALSE
