\* TEETH-B part 2 — BuggedPrefIgnoresFreshCount, PrefMonotone side. PrefFlat =
\* TRUE (override pref = 1 flat, fresh-count term dropped). QUESTION: what IS the
\* fresh-count term load-bearing for? ANSWER (this cfg proves it): the NEW
\* invariant PrefMonotone — "among override-eligible workers of the same
\* p_core_count, a larger fresh Running gives a STRICTLY WORSE (larger) pref", i.e.
\* the override preference DECAYS as the worker fills toward the ceiling (design
\* §12.1 :313-315, the "duration-of-preference" gap red-team named). With the term
\* dropped, two override workers at DIFFERENT Running have EQUAL pref (both 1), so
\* PrefMonotone MUST be VIOLATED. This pinpoints the term as load-bearing for
\* intra-override-tier fresh-count decay — NOT belt-and-suspenders.
\*
\* Topology (corrected — an earlier 3-worker/5-op attempt made PrefMonotone
\* VACUOUS: 5 ops spread across 3 p=2 workers never fill the free tier (6 slots),
\* so no worker ever ENTERS the override tier and the antecedent "two override
\* workers at distinct Running" is unreachable). 2 workers, p=2, ceiling = 2*2 = 4.
\* Free tier per worker = Running{0,1} (2 slots) => 4 free slots total. 5 ops: the
\* ranker fills the free tier (4 dispatches -> both at Running=2 = override tier),
\* then the 5th goes to the tie-order winner w1 -> w1 override at Running=3, w2
\* override at Running=2. SAME p_count=2, DIFFERENT Running => PrefMonotone's
\* antecedent is REACHED. Under PrefFlat both prefs = 1 => 1 < 1 is FALSE =>
\* PrefMonotone VIOLATED. Capacity = 4 so Running can reach 3 (< ceiling 4).
\* MUST report: "Invariant PrefMonotone is violated."

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2"}
  Ops = {"o1", "o2", "o3", "o4", "o5"}
  WorkerOrder <- Order_w123
  Capacity = 4
  MaxScore = 2
  THRESHOLD = 50
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = FALSE
  BuggedStaleRankOnly = FALSE
  PrefFlat = TRUE
  GateActive = TRUE
  AllowEvict = FALSE
  MaxToggles = 1
  PCoreCount <- PCoreCount_All2

INVARIANTS
  TypeOK
  PrefMonotone
