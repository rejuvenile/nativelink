\* TEETH: Bug = NoA5Guard — HasPHeadroom drops the p_core_count==0 guard.
\* Fleet is a SINGLE p_count=0 worker (the mis-advertising / legacy shape the
\* A5 reviewers flagged). EXPECT:
\*   - I4_HeterogeneitySafe VIOLATED: the p_count=0 worker lacks headroom.
\*   - EventuallyDispatched VIOLATED: with no P-headroom EVER and no OTHER
\*     worker, SomeViableHasPHeadroom is false so the cache tier's Phase-2
\*     branch would normally admit — but the fallback path fires... EXCEPT the
\*     I4 state-invariant already trips at Init (the worker is p_count=0 with
\*     0 running => Running(w) < 0 is FALSE => no headroom). I4 is the primary
\*     teeth signal here; the wedge is the downstream consequence in a fleet
\*     where the ONLY cache-viable requires headroom.
\* Single worker / 2 ops / capacity 2.

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1"}
  Ops = {"o1", "o2"}
  Capacity = 2
  AllowEvict = FALSE
  AllowLoadSat = FALSE
  MaxToggles = 0
  Bug = "NoA5Guard"
  PCoreCount <- PCoreCount_Zero

INVARIANTS
  TypeOK
  I4_HeterogeneitySafe
  I5_Freshness
  CapacityRespected
