\* FL-688 V2 — LOCALITY-GATED REGRESSION (addresses cadre BLOCK-2b directly):
\* the NAIVE fix that gates the durable-skip mark_stable on d IN serverLocality.
\* Under LocalityEvicted churn between produce and upload, the gate MISSES =
\* the reintroduced locality-drift class. Proves the real fix MUST be an
\* ungated GLOBAL BIS (Fixed.cfg), not a locality-gated one.
\* Expect: DurableHeldReleasedAtQuiescence VIOLATED (drift re-opens the leak).
\* StableFeedIsDurable still HOLDS (even the broken variant never feeds
\* non-durable — durability is not the axis at fault).
SPECIFICATION Spec
CONSTANTS
    Digests = {d1, d2}
    MarkStableOnDurableSkip = TRUE
    FixGatesOnLocality = TRUE
INVARIANT TypeOK
INVARIANT StableFeedIsDurable
INVARIANT DurableHeldReleasedAtQuiescence
CHECK_DEADLOCK FALSE
