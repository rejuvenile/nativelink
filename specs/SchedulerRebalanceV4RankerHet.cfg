\* V4 RANKER — heterogeneous fleet (PCoreCount_Het = {w1:1, w2:2, w3:0}). Confirms
\* I6 + carried invariants HOLD across mixed p_core_count, including an A5 (p_count=0)
\* worker (w3, always ungated) mixed with gated workers. Exercises the PrefMonotone
\* SAME-p_count restriction (w1 vs w2 differ so are not compared; A5 w3 is never
\* override-eligible so is excluded from the intra-tier monotone check). FACTOR = 2
\* => w1 ceiling 2, w2 ceiling 4. Capacity = 5 > 4 so the fresh-count ceiling binds.

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2", "w3"}
  Ops = {"o1", "o2", "o3"}
  WorkerOrder <- Order_w123
  Capacity = 5
  MaxScore = 2
  THRESHOLD = 50
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = FALSE
  BuggedStaleRankOnly = FALSE
  PrefFlat = FALSE
  GateActive = TRUE
  AllowEvict = TRUE
  MaxToggles = 2
  PCoreCount <- PCoreCount_Het

INVARIANTS
  TypeOK
  I4_HeterogeneitySafe
  I5_Freshness
  CapacityRespected
  EvictedEmpty
  NoWedge
  PrefMonotone

PROPERTIES
  I6_RankerConsistency
  I5_BoundedOverride
  I1_PHeadroomFirst
  I3_LocalityBounded
