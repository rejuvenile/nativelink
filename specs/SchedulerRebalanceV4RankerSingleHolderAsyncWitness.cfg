\* ATOMICITY BATCH D — NON-VACUITY WITNESS (W1): "evict the last holder".
\* Runs the NEGATED reachability invariant LastHolderEvictedReached, EXPECTED TO
\* BE VIOLATED. The violation trace is the positive proof that the de-atomized
\* holder set can reach EMPTY while same-root work is still Queued — the exact
\* interleaving the frozen RootHolders CONSTANT could never express. If this
\* HELD (no violation), the split atom would be dead code and the AsyncFixed HOLD
\* would be vacuous.

SPECIFICATION Spec

CONSTANTS
  Workers = {"w1", "w2"}
  Ops = {"o1", "o2", "o3"}
  WorkerOrder <- Order_w123
  InitRootHolders = {"w1"}
  Capacity = 5
  MaxScore = 1
  THRESHOLD = 101
  FACTOR = 2
  AdmitAllThreshold = FALSE
  DropCeiling = FALSE
  BuggedStaleRankOnly = FALSE
  PrefFlat = FALSE
  GateActive = TRUE
  AllowEvict = FALSE
  MaxToggles = 1
  HolderBudget = 2
  PCoreCount <- PCoreCount_All1

INVARIANTS
  LastHolderEvictedReached
