\* Reachability / non-vacuity witness for the Fixed model. Runs NeverAcked
\* as an INVARIANT expected to VIOLATE: the CE trace proves ack = "AckedOk"
\* is genuinely reachable under GateOnDurable = TRUE, so NoLossyAckCascade
\* in ReplicaInvariantAsyncFixed.cfg is not vacuously true.

SPECIFICATION Spec

CONSTANTS
    GateOnMirror = TRUE
    GateOnDurable = TRUE

INVARIANTS
    TypeOK
    NeverAcked

CHECK_DEADLOCK FALSE
