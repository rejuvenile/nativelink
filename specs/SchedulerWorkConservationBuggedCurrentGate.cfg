\* BUG REPRO — current prod gate (worker_has_p_headroom, threshold-0 collapse
\* => running < p_core_count).  Small shape: 2 workers, P=2/E=2 (4 processors
\* each), 6 queued actions, RAM never binds (RamCap=100 >> demand).
\*
\* Fleet P-capacity = 2 workers * 2 p_cores = 4 < 6 actions, so at the dispatch
\* fixpoint each worker sits at running = 2 = p_core_count with 2 E-cores idle
\* and >=2 actions still queued.
\*
\* EXPECT: WorkConserving VIOLATED — TLC prints a counterexample trace ending
\* in the "queued work + idle E-cores + RAM fits" quiescent state (the live
\* incident, in miniature).

SPECIFICATION Spec

CONSTANTS
  Workers = {w1, w2}
  Actions = {a1, a2, a3, a4, a5, a6}
  PCoreCount = 2
  ECoreCount = 2
  RamCap = 100
  ActionRamEach = 1
  GateMode = "PCoreOnly"

SYMMETRY Symmetry

INVARIANTS
  TypeOK
  NoProcessorOversubscription
  NoRamOverflow
  WorkConserving
