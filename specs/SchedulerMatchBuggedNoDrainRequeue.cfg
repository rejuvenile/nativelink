\* BUGGED variant: eviction drain drops the requeue/worker-id-unset step.
\* Expect NoOrphanExecuting (and progress) to be VIOLATED -- proving the
\* safety+liveness invariants have teeth against the orphan class.

SPECIFICATION Spec

CONSTANTS
  Workers = {w1, w2}
  Ops = {o1, o2}
  Capacity = 1
  MaxRetries = 2
  AllowEvict = TRUE

INVARIANTS
  TypeOK
  NoOrphanExecuting
  NoSlotLeak
