\* FIX — candidate total-core gate (running < p_core_count + e_core_count),
\* resource-fit still required.  Same small shape as the bug cfg: 2 workers,
\* P=2/E=2 (4 processors each), 6 queued actions, RAM never binds.
\*
\* Total-core capacity = 2 workers * 4 processors = 8 >= 6 actions, so every
\* action is placed (onto E-cores once P is full); the only fixpoints have an
\* empty queue => no idle processor coexists with fitting queued work.
\*
\* EXPECT: no error.  WorkConserving, NoProcessorOversubscription (<= 4/worker)
\* and NoRamOverflow all hold simultaneously.

SPECIFICATION Spec

CONSTANTS
  Workers = {w1, w2}
  Actions = {a1, a2, a3, a4, a5, a6}
  PCoreCount = 2
  ECoreCount = 2
  RamCap = 100
  ActionRamEach = 1
  GateMode = "TotalCore"

SYMMETRY Symmetry

INVARIANTS
  TypeOK
  NoProcessorOversubscription
  NoRamOverflow
  WorkConserving
